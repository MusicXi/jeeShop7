<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="com.zlkj.ssm.shop.manage.dao.AdminExpressDao">	<select id="selectPageList" parameterType="com.zlkj.ssm.shop.manage.entity.Express" resultType="com.zlkj.ssm.shop.manage.entity.Express">	 select * 	 from t_express t where 1=1  order by order1   limit #{offset},#{pageSize} 	</select> 	<select id="selectPageCount" parameterType="com.zlkj.ssm.shop.manage.entity.Express" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_express t where 1=1 	 ) a 	</select> 	<insert id="insert" parameterType="com.zlkj.ssm.shop.manage.entity.Express" useGeneratedKeys="true" keyProperty="id">		insert into t_express		(		id,fee		<if test="code!=null and code!=''">		,code		</if>		<if test="name!=null and name!=''">		,name		</if>		<if test="order1!=0">		,order1		</if>		)		values		(		id,#{fee}		<if test="code !=null and code !=''">		,#{code}		</if>		<if test="name !=null and name !=''">		,#{name}		</if>		<if test="order1 !=0">		,#{order1}		</if>	)</insert>	<update id="update" parameterType="com.zlkj.ssm.shop.manage.entity.Express">		update t_express		  set id=#{id},fee=#{fee}		<if test="code !=null and code !=''">		,code=#{code}		</if>		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="order1 !=0">		,order1=#{order1}		</if>		 where 1=1 and id=#{id}	</update>	<select id="selectOne" parameterType="com.zlkj.ssm.shop.manage.entity.Express" resultType="com.zlkj.ssm.shop.manage.entity.Express">	 select * from t_express t where 1=1 and id=#{id}	</select>	<delete id="deleteById" parameterType="com.zlkj.ssm.shop.manage.entity.Express">		delete from t_express where 1=1 and id=#{id}	</delete></mapper>